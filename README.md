# smart-mouse-trap-esp32
This IoT project detects mouse activity using an IR sensor connected to an ESP32 microcontroller. When the sensor detects a mouse inside the trap, the ESP32 sends a notification message to a Telegram group using a Telegram bot.
